/*global describe,it*/
'use strict';
var assert = require('assert');
//    javascriptSdk = require('../dist/javascript-sdk.js');

describe('javascript-sdk node module.', function () {
    it('must be awesome', function () {
        //assert( javascriptSdk.submit(), true);
        assert(true, true);
    });
});
